{
    "build": {
      "env": {
        "NODE_ENV": "production"
      }
    },
    "installCommand": "yarn install --check-files",
    "rewrites": [
    { "source": "/api/(.*)", "destination": "https://your-backend.vercel.app/api/$1" }
  ]
  }
  